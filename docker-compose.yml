services:
  whatsapp-bot:
    build: .
    container_name: whatsapp-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - wa-session:/app/.wa_auth
      - wa-data:/app/data
    # QR code / pairing code for first-time login is printed to logs: docker compose logs -f

volumes:
  wa-session:
  wa-data:
